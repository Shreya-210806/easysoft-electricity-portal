# MongoDB connection string. Include database name to use/create that database.
# If not set, defaults to: mongodb://localhost:27017/portalpal
MONGO_URI=mongodb://localhost:27017/portalpal

# Server port (default: 5001 — avoid 5000, used by macOS AirPlay)
PORT=5001
